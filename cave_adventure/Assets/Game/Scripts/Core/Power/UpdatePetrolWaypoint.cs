using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace RPG.Core
{
    public class UpdatePetrolWaypoint : MonoBehaviour
    {
        // required variables
        [SerializeField] GameObject[] petrolPaths;

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
